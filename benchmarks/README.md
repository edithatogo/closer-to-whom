# Benchmarks

Benchmarks protect against accidental algorithmic regressions; they are not scientific performance claims. Correctness is checked before timing, budgets are deliberately generous across CI hardware, and baseline changes require a reviewed receipt. Publication-critical acceleration is promoted only after oracle, property, differential, numerical-tolerance, determinism, memory, and end-to-end benchmarks pass.
