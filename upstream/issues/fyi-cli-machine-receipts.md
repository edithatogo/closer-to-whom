# feat: machine-readable OIA request and response receipts

Add request manifests, minimisation fields, agency/status tracking, rendered request hash, submission receipt, response/attachment checksums, licence notes, supersession, and Arrow/JSON metadata export. The CLI remains optional to downstream model execution.

Project oracle: `data/public/oia-requests.yaml` and `src/closer_to_whom/integrations/fyi.py`.
