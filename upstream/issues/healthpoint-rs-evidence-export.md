# feat: licensed capability-evidence export with fail-closed publication controls

Add a typed export for facility/service claims, source timestamps, evidence categories, and licence grant metadata. Require separate booleans for local access, local analysis, repository redistribution, and public dashboard use. Default outputs to private paths and synthetic fixtures; never infer permission from successful API access.

Project oracle: `src/closer_to_whom/integrations/healthpoint.py`.
