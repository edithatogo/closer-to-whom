# Generated artefacts

`artifacts/demo/` contains deterministic synthetic outputs needed for dashboard and package smoke tests. All other artefacts are ignored by Git. Publication outputs must include a manifest, input and assumptions fingerprints, verification receipt, and the exact code revision.
